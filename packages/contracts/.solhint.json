{
  "extends": "solhint:default",
  "plugins": [],
  "rules": {
    "avoid-suicide": "error",
    "avoid-sha3": "warn",
    "compiler-version": ["error", "^0.7.0"],
    "func-visibility": ["error", { "ignoreConstructors": true }]
  }
}
